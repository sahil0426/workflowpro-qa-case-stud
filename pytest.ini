[pytest]
# Retry tests marked as known-transient (infra flakiness), not real bugs.
addopts = -v --reruns 1 --reruns-delay 2
markers =
    flaky_known: known-transient infra flakiness, safe to auto-retry
    smoke: fast subset of tests for quick sanity checks
